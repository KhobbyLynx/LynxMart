// ** Logo
import { images } from '../constants'

export const appConfig = {
  app: {
    appName: 'LynxMart',
    appLogoImage: images.logo,
  },
  layout: {
    scrollToTop: true, // true or false
  },
}
